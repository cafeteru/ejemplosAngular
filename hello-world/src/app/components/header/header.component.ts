import {Component} from '@angular/core';

@Component({
  selector: 'app-header', // nombre de la etiqueta
  templateUrl: './header.component.html' // plantilla html que se carga dentro del selector
})

export class HeaderComponent {

}
